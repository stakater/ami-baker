{
  "variables": {
    "aws_region": "",
    "source_ami": "",
    "ami_name": "",
    "instance_type": "",
    "subnet_id":"",
    "vpc_id":""
  },
  "builders": [{
    "type": "amazon-ebs",
    "region": "{{user `aws_region`}}",
    "subnet_id":"{{user `subnet_id`}}",
    "vpc_id":"{{user `vpc_id`}}",
    "source_ami": "{{user `source_ami`}}",
    "user_data_file":"cloud-config/cloud-config.yaml",
    "instance_type": "{{user `instance_type`}}",
    "ssh_username": "core",
    "ami_name": "{{user `ami_name`}}"
  }]
}